apiVersion: v1
kind: Pod
metadata:
  name: my-nginx
  namespace: argocd
  labels:
    app: nginx
spec:
  containers:
    - name: nginx
      image: harbor-dw.cowave.kr/argocd-dev/nginx
      ports:
        - containerPort: 80
  imagePullSecrets:
    - name: argo-secret
